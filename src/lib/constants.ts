// Shared between server (cookie) and client (localStorage + document.cookie)
// code, so this file must stay free of server-only or client-only imports.
export const SELECTED_VEHICLE_COOKIE_NAME = "selected_vehicle_id";
export const SELECTED_VEHICLE_STORAGE_KEY = "mt:selectedVehicleId";
